# TythanAI v6.5 — Core dependencies

# ── Core ──────────────────────────────────────────────────────────────
pydantic>=2.0.0
PyYAML>=6.0.1
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0

# ── SAST / SCA scanners ───────────────────────────────────────────────
semgrep>=1.100.0
bandit>=1.7.7

# ── API server (optional — needed only if running the REST API) ────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# ── Vector memory (optional — ChromaDB for rule deduplication) ────────
chromadb>=0.4.24

# ── AI / LLM (optional — for AI auto-fix features) ───────────────────
# openai>=1.30.0

# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
